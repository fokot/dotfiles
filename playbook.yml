---
- name: Configure macOS
  hosts: localhost
  connection: local
  gather_facts: yes

  vars:
    brew_prefix: /opt/homebrew

  tasks:
    - name: Tap sdkman/tap
      community.general.homebrew_tap:
        name: sdkman/tap

    - name: Install Homebrew formulas
      community.general.homebrew:
        name:
          - sdkman-cli
          - nvm
          - fzf
          - gnupg
          - libpq
          - mas
          - kubernetes-cli
          - kubectx
          - orbstack
        state: present

    - name: Install Homebrew casks
      community.general.homebrew_cask:
        name:
          - raycast
          - jetbrains-toolbox
          - font-sauce-code-pro-nerd-font
          - google-chrome
          - freelens
          - headlamp
          - zed
        state: present

    - name: Symlink kubectx as kubectl-ctx
      ansible.builtin.file:
        src: "{{ brew_prefix }}/bin/kubectx"
        dest: "{{ brew_prefix }}/bin/kubectl-ctx"
        state: link
        force: yes

    - name: Symlink kubens as kubectl-ns
      ansible.builtin.file:
        src: "{{ brew_prefix }}/bin/kubens"
        dest: "{{ brew_prefix }}/bin/kubectl-ns"
        state: link
        force: yes

    - name: Install Claude Code
      ansible.builtin.shell: curl -fsSL https://claude.ai/install.sh | bash
      args:
        creates: "{{ ansible_facts['env']['HOME'] }}/.local/bin/claude"

    - name: Run fzf install script
      ansible.builtin.command: "{{ brew_prefix }}/opt/fzf/install --all --no-update-rc"
      args:
        creates: "{{ ansible_facts['env']['HOME'] }}/.fzf.zsh"

    - name: Install oh-my-zsh
      ansible.builtin.shell: 'sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)" "" --unattended'
      args:
        creates: "{{ ansible_facts['env']['HOME'] }}/.oh-my-zsh"
      environment:
        RUNZSH: "no"
        CHSH: "no"

    - name: Clone powerlevel10k theme
      ansible.builtin.git:
        repo: https://github.com/romkatv/powerlevel10k.git
        dest: "{{ ansible_facts['env']['HOME'] }}/.oh-my-zsh/custom/themes/powerlevel10k"
        depth: 1
        update: no

    - name: Set ZSH_THEME to powerlevel10k
      ansible.builtin.lineinfile:
        path: "{{ ansible_facts['env']['HOME'] }}/.zshrc"
        regexp: "^ZSH_THEME="
        line: 'ZSH_THEME="powerlevel10k/powerlevel10k"'

    - name: Manage zshrc block
      ansible.builtin.blockinfile:
        path: "{{ ansible_facts['env']['HOME'] }}/.zshrc"
        marker: "# {mark} ANSIBLE MANAGED BLOCK — mac bootstrap"
        block: "{{ lookup('ansible.builtin.template', 'templates/zshrc.block.j2') }}"
        create: yes

    - name: Set git pull-current alias
      community.general.git_config:
        scope: global
        name: alias.pull-current
        value: "!git pull origin $(git branch --show-current)"

    - name: List installed App Store apps
      ansible.builtin.command: mas list
      register: mas_list
      changed_when: false

    - name: Extract installed App Store IDs
      ansible.builtin.set_fact:
        installed_app_ids: "{{ mas_list.stdout_lines | map('split') | map('first') | list }}"

    - name: Check if Slack.app exists in /Applications
      ansible.builtin.stat:
        path: /Applications/Slack.app
      register: slack_app

    - name: Install Slack from App Store
      ansible.builtin.command: mas install 803453959
      become: yes
      when:
        - "'803453959' not in installed_app_ids"
        - not slack_app.stat.exists

    - name: Check if WhatsApp.app exists in /Applications
      ansible.builtin.stat:
        path: /Applications/WhatsApp.app
      register: whatsapp_app

    - name: Install WhatsApp from App Store
      ansible.builtin.command: mas install 310633997
      become: yes
      when:
        - "'310633997' not in installed_app_ids"
        - not whatsapp_app.stat.exists
